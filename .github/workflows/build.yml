# Deprecated — this workflow targeted the old `app/` directory.
# Active release pipeline: .github/workflows/build-release.yml
name: Build/Release CineStream (legacy)

on:
  workflow_dispatch:
    inputs:
      confirm:
        description: 'Type RUN to execute the legacy app/ pipeline'
        required: true

jobs:
  skipped:
    if: github.event.inputs.confirm == 'RUN'
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: echo "Legacy workflow — prefer build-release.yml for app-v2"
